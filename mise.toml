[tools]
rust = "latest"

[tasks.install]
description = "Build and install tide"
run = "cargo install --path ."

[tasks.build]
description = "Build tide (debug)"
run = "cargo build"

[tasks.check]
description = "Run clippy and check"
run = "cargo clippy -- -D warnings"
